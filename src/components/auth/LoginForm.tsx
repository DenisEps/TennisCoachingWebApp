'use client';

import { useRouter } from 'next/navigation';

const LoginForm = () => {
  const router = useRouter();

  return (
    <div>
      {/* Component content */}
    </div>
  );
};

export default LoginForm; 